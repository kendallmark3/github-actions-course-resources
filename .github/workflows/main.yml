# .github/workflows/main.yml - Barebones workflow for testing parsing
name: Basic Workflow Test

on:
  push:
    branches:
      - main

jobs:
  run-simple-command:
    runs-on: ubuntu-latest
    steps:
      - name: Get code
        uses: actions/checkout@v3 # Checks out your repository's code

      - name: Run a simple command
        run: echo "Hello, GitHub Actions! This workflow ran successfully."